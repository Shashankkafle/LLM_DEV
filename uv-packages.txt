# SUMO TraCI
traci

# Pinned (stable, confirmed on PyPI)
annotated-doc==0.0.4
anyio==4.13.0
certifi==2025.4.26
click==8.1.8
colorama==0.4.6
filelock==3.16.1
h11>=0.16
httpcore>=1.0.0
httpx>=0.27.0
idna==3.10
jinja2==3.1.6
markdown-it-py==3.0.0
markupsafe==3.0.2
mdurl==0.1.2
mpmath==1.3.0
packaging==24.2
psutil==6.1.1
pygments==2.19.1
pyyaml==6.0.2
rich==13.9.4
setuptools==75.8.2
shellingham==1.5.4
sympy==1.13.3
tqdm==4.67.1
typer==0.15.1
typing-extensions==4.12.2

# Relaxed (original versions didn't exist on PyPI)
accelerate>=0.34.0
fsspec>=2024.6.0
hf-xet>=1.0.0
huggingface-hub>=0.24.0
networkx>=3.3
numpy>=1.26.0
regex>=2024.5.15
safetensors>=0.4.3
tokenizers>=0.19.0
# torch: install SEPARATELY before this file, with an explicit CUDA build, e.g.
#   pip install "torch>=2.3.0" --index-url https://download.pytorch.org/whl/cu121
# The cu121 wheel runs on any machine whose driver supports CUDA >= 12.1, which
# covers all our nodes. It is kept out of this list so the bulk install never
# pulls the PyPI-default wheel (currently a CUDA-13 build that breaks on our drivers).
transformers>=4.43.0

# CoLight RL baseline -- TensorFlow/Keras agent (lifted verbatim from LLMTSCS).
# tf-keras + TF_USE_LEGACY_KERAS=1 (set in models_inference/RL/colight_agent.py) give
# Keras-2 semantics on modern TF so the verbatim Keras-2 code runs. Pulls in the TF
# transitive deps (keras, h5py, grpcio, protobuf, ml_dtypes, ...) via uv. numpy 2.x /
# torch are unaffected (verified: no downgrades). Needed for runner_colight.py.
tensorflow-cpu==2.21.0
tf-keras==2.21.0