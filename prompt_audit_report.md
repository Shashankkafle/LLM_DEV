# C3 prompt audit

Generated by `audit_prompts.py`, 2026-07-28T10:25:45.
Scenario `dataset/llm_light/Hangzhou/4_4/scenarios/c3_through_north_2_3_1200s.json`: obstacle on `road_2_4_3_1` at position 10.0, active steps [500, 1700).

## Runs audited


**Missing: +text (both reports), partial (approach-side report only), -text (--hide_blockage_info), normal conditions (no blockage).** Searched `logs` for run manifests with `controller == "llm"` and a `c3*` blockage scenario at seed 1. Every check over a missing run reports NO DATA -- that is an absence of evidence, not a pass. Point the audit at the run dirs with `--logs-dir`, `--seed`, or the explicit `--text-run` / `--partial-run` / `--notext-run` / `--normal-run` flags.

## Existing test coverage

How much of each check the committed tests already pin. Template-level coverage constrains the builder, not the prompts a run actually emitted, so it reduces but does not remove the need for the run-level check.

| Check | Existing coverage | Detail |
|---|---|---|
| CHECK 1 - golden blockage blocks at 2_3 / 2_4, absent elsewhere | PARTIAL (template + run, different scenario) | test_prompt_builder.test_blockage_section_golden / test_exit_blockage_section_golden pin both block texts at template level, on hand-built fixtures. smoke_event_text_matrix T1 does the run-level equivalent (exact section diff at the two named intersections, section-free everywhere else) but for S1/S2/S3/S4 on a 400-step stub-LLM run, never for C3 and never at 2_3 / 2_4. The C3 goldens themselves are pinned nowhere. |
| CHECK 1 - the '563 m ... 573 m link' slot values | PARTIAL (template, same numbers, different lane) | test_exit_blockage_section_golden pins '563 m past this intersection on a 573 m link' for EXIT_BLOCKAGE_SOUTH (road_1_4_3_0, distance_m 562.8, lane_length_m 572.8 fed in as fixture constants). smoke_event_text_matrix derives the same numbers live for road_1_4_3. Neither touches road_2_4_3_1, and no test derives the numbers from the net file. |
| CHECK 2 - blockage sections only inside the active window | PARTIAL (run, different scenario) | smoke_event_text_matrix.check_scenario_sections asserts pre-window prompts are section-free (window start 50, S1-S4). No test checks the window END, and none uses C3's [500, 1700). |
| CHECK 3 - approach-only arm (--blockage_info_scope approach) | NONE | No test sets blockage_info_scope to anything but 'both' (verified by grep across tests/): smoke_blockage_llm_wiring, smoke_blockage_prompt_leakage, smoke_event_text_matrix, test_run_record_schema and verify_batch_loop_equivalence all hard-code 'both'. There is no golden and no run-level test for the partial arm. |
| CHECK 4 - -text arm carries no blockage text; equals the clean run | COVERED at template + run level, on the toy net | smoke_blockage_prompt_leakage (T8a) is exactly this check: A-vs-B byte-identical user prompts for a whole run, no blockage vocabulary in B, cadence equality. But it runs on simulations/single_intersection with demand engineered so the obstacle cannot perturb traffic. smoke_event_text_matrix T2 adds 'L1 arm never carries a section' on hangzhou 4x4. Neither is C3, and neither faces the real case where the blockage genuinely changes traffic. |
| CHECK 5 - system prompt frozen and identical across arms | COVERED at template level; PARTIAL at run level | test_prompt_builder.test_system_prompt_is_pinned and test_commonsense_block_is_pinned pin both strings verbatim. smoke_event_text_matrix T4 checks the http_llm._format_prompt seam and that the block never leaks into a logged user prompt. Neither reads back what a real run recorded in run_manifest.json (llm.system_prompt / llm.example_formatted_prompt). |
| CHECK 6 - position semantics vs. the SUMO network | PARTIAL (run, different lane) | smoke_event_text_matrix T3 checks link_length - distance == 10 within rounding for road_1_4_3, from the prompt text alone. No test opens the net file, and none covers road_2_4_3_1. |

## Results

| Check | Verdict |
|---|---|
| CHECK 1 - golden blockage blocks in the +text run | **NO DATA** |
| CHECK 2 - window discipline in the +text run | **NO DATA** |
| CHECK 3 - approach-only run | **NO DATA** |
| CHECK 4 - -text run | **NO DATA** |
| CHECK 5 - system prompt | **NO DATA** |
| CHECK 6 - position semantics | **PASS** |

### CHECK 1 - golden blockage blocks in the +text run - NO DATA

- NO DATA: no +text run dir available

### CHECK 2 - window discipline in the +text run - NO DATA

- NO DATA: no +text run dir available

### CHECK 3 - approach-only run - NO DATA

- NO DATA: no partial (approach-only) run dir available

### CHECK 4 - -text run - NO DATA

- NO DATA: no -text run dir available

### CHECK 5 - system prompt - NO DATA

- decisions.jsonl stores only the user prompt; the system prompt a run used is recorded in run_manifest.json under llm.system_prompt, with llm.example_formatted_prompt holding the fully templated first prompt.
- NO DATA: no run manifest records a system prompt

### CHECK 6 - position semantics - PASS

- Net: dataset/llm_light/Hangzhou/4_4/roadnet.net.xml
- Edge road_2_4_3 runs intersection_2_4 -> intersection_2_3; 3 lanes; lane road_2_4_3_1 is index 1, length 572.80 m
- Convention found: a scenario's `position` is metres UPSTREAM of the STOP LINE, not metres from the lane start. utils/blockage_manager._lane_position_from_stopline converts it for TraCI as lane_pos = lane_length - position, and SumoEnv.describe_exit_blockages reports distance_m = lane_length - position as the distance from the UPSTREAM intersection. So position 10.0 m puts the obstacle at SUMO lane position 562.8 m (measured from the lane start at intersection_2_4), i.e. 10 m before the stop line at intersection_2_3 and 562.8 m past intersection_2_4.
- net geometry reproduces the golden claim exactly: "563 m past this intersection on a 573 m link"
- golden's '1 of 3 lanes' matches the edge's lane count
- segment 1 boundary is length/10 = 57.28 m, so position 10.0 m is in segment 1 -- matching the golden approach block
- cross-check: logs\c3_decision\mp_hz1_c3_seed1_20260722_173634 froze the obstacle at SUMO lane position 562.8 m at t=511, matching lane_length - position = 562.8 m
- note: in logs\c3_decision\mp_hz1_c3_seed1_20260722_173634 placement was deferred to t=511 (spot occupied), while the blockage counts as active -- and is reported in prompts -- from t=500
- cross-check: logs\c3_decision\mp_hz1_c3_seed2_20260722_174145 froze the obstacle at SUMO lane position 562.8 m at t=511, matching lane_length - position = 562.8 m
- note: in logs\c3_decision\mp_hz1_c3_seed2_20260722_174145 placement was deferred to t=511 (spot occupied), while the blockage counts as active -- and is reported in prompts -- from t=500
- cross-check: logs\loadfix_c3_20260728_094436 froze the obstacle at SUMO lane position 562.8 m at t=500, matching lane_length - position = 562.8 m

